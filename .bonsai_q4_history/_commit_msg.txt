Removed 2 redundant bufferBarrier calls: one at end of encodeAttnProjFusedNorm and one at end of encodeAttnProjSeparateNorm. Both were duplicated by the caller encodeAttentionProjections. Saves 28 barriers per forward pass (one per decoder layer). Result: small but consistent improvement ~213 → ~218 tok/s. Parity with MLX reference improved from 90% to 92%.